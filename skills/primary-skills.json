[
  {
    "type": "function",
    "function": {
      "name": "memorise",
      "description": "Stores a fact or piece of information you want to remember during conversations and or action output you want to remember for further action taking.",
      "parameters": {
        "type": "object",
        "properties": {
          "fact": {
            "type": "string",
            "description": "The fact or information you want to remember."
          }
        },
        "required": ["fact"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "search_skills",
      "description": "Searches all available skill schemas for tools matching a given tag and returns their names and descriptions.",
      "parameters": {
        "type": "object",
        "properties": {
          "tag": {
            "type": "string",
            "description": "The tag to search for (e.g., 'notion', 'memory', 'github')."
          }
        },
        "required": ["tag"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "load_skills",
      "description": "Loads full tool schemas by name so they become callable. Resets previously loaded tools.",
      "parameters": {
        "type": "object",
        "properties": {
          "names": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Tool names to load."
          }
        },
        "required": ["names"]
      }
    }
  }
]
